# intentional_error.py

numbers = [1, 2, 3, 4, 5]

# Intentional index error
print(numbers[10])  # This will produce an index error

# Fix the error by accessing a valid index
print(numbers[3])   # Access a valid index
